{# templates/admin/size/form.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}{{ form.vars.value.id is null ? 'Ajouter' : 'Modifier' }} une Taille{% endblock %}

{% block body %}
    <h1>{{ form.vars.value.id is null ? 'Ajouter' : 'Modifier' }} une Taille</h1>
    
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}

    <a href="{{ path('admin_size_index') }}" class="btn btn-secondary">Retour à la liste des tailles</a>
{% endblock %}
