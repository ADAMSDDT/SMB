<aside class="col-3 border border-light bg-white p-2">
    <article class="card mb-3">
        <div class="card-header">
            Catégories
        </div>
        <div class="card-body">
            <p><a href="{{ path('admin_categories_index') }}">Liste des catégories</a></p>
            <p><a href="">Ajouter une catégorie</a></p>
        </div>
    </article>
    <article class="card mb-3">
        <div class="card-header">
            Produits
        </div>
        <div class="card-body">
            <p><a href="{{ path('admin_products_index') }}">Liste des produits</a></p>
            <p><a href="{{ path('admin_products_add') }}">Ajouter un produit</a></p>
        </div>
    </article>
    <article class="card mb-3">
        <div class="card-header">
            Tailles
        </div>
        <div class="card-body">
            <p><a href="{{ path('admin_size_index') }}">Liste des tailles</a></p>
            <p><a href="{{ path('admin_size_add') }}">Ajouter une taille</a></p> {# Assurez-vous que la route 'admin_size_add' existe #}
        </div>
    </article>
    <article class="card mb-3">
        <div class="card-header">
            Utilisateurs
        </div>
        <div class="card-body">
            <p><a href="{{ path('admin_users_index') }}">Liste des utilisateurs</a></p>
        </div>
    </article>
</aside>
